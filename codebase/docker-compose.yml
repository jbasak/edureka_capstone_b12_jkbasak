services:
  procurement-agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - model_cache:/root/.cache
    restart: unless-stopped

volumes:
  model_cache: